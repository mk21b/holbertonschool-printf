# Holberton printf Project

## Description
This project implements a custom `_printf` function that mimics the C standard library function printf. It supports conversion specifiers: %c, %s, %d, %i, and %%.

## Compilation
Compile the files with GCC as follows:


